# Retired GoAnime multi-provider v2 trigger path

The Dart multi-provider v2 refresh was retired on 2026-08-18. Historical `.request` files remain only as provenance.

Do not add new requests here. Provider crawling and cache publication now belong exclusively to:

```text
.github/workflows/goanime-scrapling-provider-cache.yml
```

New source-bound refresh requests belong in `triggers/goanime-scrapling-provider-cache/`.
