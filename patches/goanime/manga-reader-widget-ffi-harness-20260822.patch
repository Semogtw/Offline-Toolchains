diff --git a/lib/screens/manga/manga_reader_screen.dart b/lib/screens/manga/manga_reader_screen.dart
index 7116d5f..7e96d4a 100644
--- a/lib/screens/manga/manga_reader_screen.dart
+++ b/lib/screens/manga/manga_reader_screen.dart
@@ -113,7 +113,7 @@ class _MangaReaderScreenState extends State<MangaReaderScreen> {
       }
     }
     unawaited(_loadReaderMode());
-    unawaited(WakelockPlus.enable());
+    unawaited(_setWakelock(enabled: true));
     if (widget.manifest is! LocalPackageContentManifest) {
       _recordPage(_pageIndex, _pageCount);
     }
@@ -137,10 +137,23 @@ class _MangaReaderScreenState extends State<MangaReaderScreen> {
     _imageController?.dispose();
     _continuousController?.dispose();
     _pdfController = null;
-    unawaited(WakelockPlus.disable());
+    unawaited(_setWakelock(enabled: false));
     super.dispose();
   }
 
+  Future<void> _setWakelock({required bool enabled}) async {
+    try {
+      if (enabled) {
+        await WakelockPlus.enable();
+      } else {
+        await WakelockPlus.disable();
+      }
+    } catch (_) {
+      // Wakelock is supplemental. Unsupported platform integration must not
+      // prevent a chapter from opening or closing cleanly.
+    }
+  }
+
   Future<void> _loadReaderMode() async {
     try {
       final override = await _preferencesRepository.readerModeOverrideForWork(
diff --git a/test/widgets/manga/manga_reader_screen_test.dart b/test/widgets/manga/manga_reader_screen_test.dart
index d514fef..176eb75 100644
--- a/test/widgets/manga/manga_reader_screen_test.dart
+++ b/test/widgets/manga/manga_reader_screen_test.dart
@@ -69,10 +69,14 @@ void main() {
   testWidgets('can clear a work override and return to the global mode', (
     tester,
   ) async {
-    await preferences.saveDefaultReaderMode(MangaReaderMode.pagedRtl);
-    await preferences.saveReaderMode(
-      'work-1',
-      MangaReaderMode.continuousVertical,
+    await tester.runAsync(
+      () => preferences.saveDefaultReaderMode(MangaReaderMode.pagedRtl),
+    );
+    await tester.runAsync(
+      () => preferences.saveReaderMode(
+        'work-1',
+        MangaReaderMode.continuousVertical,
+      ),
     );
 
     await tester.pumpWidget(
@@ -82,7 +86,7 @@ void main() {
         loader: (_) async => MangaLocalImageReaderContent(<File>[pageFile]),
       ),
     );
-    await _pumpUntilFound(tester, find.byTooltip('Reading mode'));
+    await _pumpUntilFound(tester, find.byIcon(Icons.view_stream_outlined));
 
     await tester.tap(find.byTooltip('Reading mode'));
     await _pumpUntilFound(tester, find.text('Use default mode'));
@@ -95,11 +99,17 @@ void main() {
     );
 
     expect(find.text('This work now uses the default mode.'), findsOneWidget);
-    expect(await preferences.readerModeOverrideForWork('work-1'), isNull);
-    expect(
-      await preferences.readerModeForWork('work-1'),
-      MangaReaderMode.pagedRtl,
+    final override = await tester.runAsync(
+      () => preferences.readerModeOverrideForWork('work-1'),
     );
+    final effectiveMode = await tester.runAsync(
+      () => preferences.readerModeForWork('work-1'),
+    );
+    expect(override, isNull);
+    expect(effectiveMode, MangaReaderMode.pagedRtl);
+
+    await tester.pumpWidget(const SizedBox.shrink());
+    await _yieldToIo(tester);
   });
 
   testWidgets('local package failure exposes retry and reloads the package', (
@@ -131,7 +141,11 @@ void main() {
       findsOneWidget,
     );
     expect(find.text('Try again'), findsOneWidget);
-    expect(await progress.progressForChapter('work-1', 'chapter-1'), isNull);
+    await _yieldToIo(tester);
+    final failedProgress = await tester.runAsync(
+      () => progress.progressForChapter('work-1', 'chapter-1'),
+    );
+    expect(failedProgress, isNull);
 
     await tester.tap(find.text('Try again'));
     await _pumpUntil(
@@ -153,7 +167,9 @@ void main() {
     MangaChapterProgress? saved;
     for (var i = 0; i < 40 && saved == null; i += 1) {
       await tester.pump(const Duration(milliseconds: 50));
-      saved = await progress.progressForChapter('work-1', 'chapter-1');
+      saved = await tester.runAsync<MangaChapterProgress?>(
+        () => progress.progressForChapter('work-1', 'chapter-1'),
+      );
     }
     expect(saved, isNotNull);
     expect(saved!.bookmark, isNotNull);
@@ -162,6 +178,9 @@ void main() {
     expect(saved.bookmark!.chapterId, 'chapter-source-1');
     expect(saved.bookmark!.pageIndex, 0);
     expect(saved.bookmark!.pageCount, 1);
+
+    await tester.pumpWidget(const SizedBox.shrink());
+    await _yieldToIo(tester);
   });
 }
 
@@ -177,10 +196,17 @@ Future<void> _pumpUntil(
   for (var i = 0; i < maxPumps; i += 1) {
     await tester.pump(const Duration(milliseconds: 50));
     if (condition()) return;
+    await _yieldToIo(tester);
   }
   throw TestFailure('Timed out waiting for the expected reader state.');
 }
 
+Future<void> _yieldToIo(WidgetTester tester) async {
+  await tester.runAsync(() async {
+    await Future<void>.delayed(const Duration(milliseconds: 10));
+  });
+}
+
 Widget _readerApp({
   required MangaPreferencesRepository preferences,
   required MangaProgressRepository progress,
