name: Observe GoAnime post-10 final squash

on:
  push:
    branches: [main]
    paths:
      - 'triggers/observe-goanime-post10-final-squash/*.request'

permissions:
  actions: read
  contents: write

jobs:
  observe:
    runs-on: ubuntu-24.04
    steps:
      - name: Capture sanitized final-squash state
        env:
          GH_TOKEN: ${{ github.token }}
          REPO: ${{ github.repository }}
        shell: bash
        run: |
          set -euo pipefail
          response="$(gh api "repos/$REPO/actions/workflows/publish-goanime-post10-final-squash.yml/runs?per_page=5")"
          report="$(jq -c '{schemaVersion:1,runs:[.workflow_runs[] | {id,status,conclusion,event,head_sha,created_at,updated_at}]}' <<<"$response")"
          encoded="$(printf '%s\n' "$report" | base64 -w0)"
          path="results/observe-goanime-post10-final-squash/${GITHUB_RUN_ID}.json"
          gh api --method PUT "repos/$REPO/contents/$path" -f message="ci(observe): record GoAnime post-10 final-squash state $GITHUB_RUN_ID" -f content="$encoded" >/dev/null
