name: Retired GoAnime Dart multi-provider catalog

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-24.04
    steps:
      - name: Retired workflow
        run: |
          echo 'This legacy Dart multi-provider refresh is retired.'
          echo 'Use .github/workflows/goanime-scrapling-provider-cache.yml instead.'
