name: Diagnose GoAnime manga staging v2 encrypted

on:
  push:
    branches:
      - main
    paths:
      - 'triggers/goanime-manga-staging-v2-diagnostic/*.request'

permissions:
  contents: read

concurrency:
  group: goanime-manga-staging-v2-diagnostic
  cancel-in-progress: false

jobs:
  diagnose:
    name: GoAnime manga staging v2 encrypted diagnostic
    if: github.actor == github.repository_owner
    runs-on: ubuntu-24.04
    timeout-minutes: 40

    steps:
      - name: Require private repository token
        env:
          PRIVATE_REPOSITORIES_TOKEN: ${{ secrets.PRIVATE_REPOSITORIES_TOKEN }}
        run: test -n "$PRIVATE_REPOSITORIES_TOKEN"

      - name: Checkout control files
        uses: actions/checkout@v6
        with:
          path: toolchain-control
          fetch-depth: 2
          persist-credentials: false
          show-progress: false

      - name: Resolve exact private revision once
        id: request
        shell: bash
        env:
          PRIVATE_REPOSITORIES_TOKEN: ${{ secrets.PRIVATE_REPOSITORIES_TOKEN }}
        run: |
          set -euo pipefail
          request="$(git -C toolchain-control diff-tree --no-commit-id --name-only --diff-filter=A -r HEAD^ HEAD -- 'triggers/goanime-manga-staging-v2-diagnostic/*.request' | head -n 1)"
          test -n "$request"
          read -r project branch requested < "toolchain-control/$request"
          test "$project" = "goanime"
          test "$branch" = "feature/manga-platform"
          if [[ "$requested" == "latest" ]]; then
            remote="https://x-access-token:${PRIVATE_REPOSITORIES_TOKEN}@github.com/Semogtw/goanime-mobile.git"
            resolved="$(git ls-remote "$remote" refs/heads/feature/manga-platform | cut -f1)"
          else
            resolved="$requested"
          fi
          [[ "$resolved" =~ ^[0-9a-f]{40}$ ]]
          echo "sha=$resolved" >> "$GITHUB_OUTPUT"

      - name: Checkout resolved private revision
        uses: actions/checkout@v6
        with:
          repository: Semogtw/goanime-mobile
          ref: ${{ steps.request.outputs.sha }}
          token: ${{ secrets.PRIVATE_REPOSITORIES_TOKEN }}
          path: private-source
          fetch-depth: 1
          persist-credentials: false
          lfs: false
          submodules: false
          show-progress: false

      - name: Set up Flutter 3.44.1
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          flutter-version: 3.44.1
          cache: true

      - name: Resolve dependencies privately
        working-directory: private-source
        run: flutter pub get >/dev/null 2>&1

      - name: Capture staging v2 diagnostics and formatted files
        id: encrypt
        working-directory: private-source
        shell: bash
        env:
          EXPECTED_FINGERPRINT: 2DE29DC31427CF0A911AB96175679291435059B0
        run: |
          set -euo pipefail
          root="$RUNNER_TEMP/goanime-manga-staging-v2-diagnostic"
          plain="$root/plain"
          formatted="$plain/formatted"
          transfer="$root/transfer"
          archive="$root/staging-v2-diagnostic.tar.zst"
          gpg_home="$root/gpg"
          rm -rf "$root"
          mkdir -p "$plain" "$formatted" "$transfer" "$gpg_home"
          chmod 700 "$gpg_home"

          set +e
          flutter test --no-pub test/services/manga/manga_http_client_test.dart >"$plain/http.log" 2>&1
          printf 'http=%s\n' "$?" >> "$plain/STATUS.txt"
          flutter test --no-pub test/services/manga/providers >"$plain/providers.log" 2>&1
          printf 'providers=%s\n' "$?" >> "$plain/STATUS.txt"
          flutter test --no-pub test/services/manga/manga_readability_verifier_test.dart >"$plain/readability.log" 2>&1
          printf 'readability=%s\n' "$?" >> "$plain/STATUS.txt"
          flutter test --no-pub test/tools/manga >"$plain/manga-tooling.log" 2>&1
          printf 'manga_tooling=%s\n' "$?" >> "$plain/STATUS.txt"
          flutter test --no-pub test/architecture/manga_planned_provider_definitions_test.dart >"$plain/planned-accounting.log" 2>&1
          printf 'planned_accounting=%s\n' "$?" >> "$plain/STATUS.txt"

          dart format \
            lib/services/manga/manga_http_client.dart \
            lib/services/manga/providers \
            test/services/manga/manga_http_client_test.dart \
            test/services/manga/providers \
            tools/manga \
            test/tools/manga \
            test/architecture/manga_planned_provider_definitions_test.dart >"$plain/format.log" 2>&1
          printf 'format=%s\n' "$?" >> "$plain/STATUS.txt"
          git diff -- \
            lib/services/manga/manga_http_client.dart \
            lib/services/manga/providers \
            test/services/manga/manga_http_client_test.dart \
            test/services/manga/providers \
            tools/manga \
            test/tools/manga \
            test/architecture/manga_planned_provider_definitions_test.dart >"$plain/format.patch"
          set -e

          while IFS= read -r path; do
            test -n "$path"
            mkdir -p "$formatted/$(dirname "$path")"
            cp "$path" "$formatted/$path"
          done < <(git diff --name-only -- \
            lib/services/manga/manga_http_client.dart \
            lib/services/manga/providers \
            test/services/manga/manga_http_client_test.dart \
            test/services/manga/providers \
            tools/manga \
            test/tools/manga \
            test/architecture/manga_planned_provider_definitions_test.dart)

          tar -C "$plain" -I 'zstd -T0 -6' -cf "$archive" .
          export GNUPGHOME="$gpg_home"
          gpg --batch --import "$GITHUB_WORKSPACE/toolchain-control/keys/source-bundles-public.asc" >/dev/null 2>&1
          fingerprint="$(gpg --batch --with-colons --fingerprint "$EXPECTED_FINGERPRINT" | awk -F: '$1 == "fpr" { print $10; exit }')"
          test "$fingerprint" = "$EXPECTED_FINGERPRINT"
          gpg --batch --yes --trust-model always --recipient "$EXPECTED_FINGERPRINT" \
            --output "$transfer/staging-v2-diagnostic.gpg" --encrypt "$archive"
          sha256sum "$transfer/staging-v2-diagnostic.gpg" > "$transfer/SHA256SUMS"

          rm -rf "$plain" "$archive" "$gpg_home" "$GITHUB_WORKSPACE/private-source"
          echo "transfer=$transfer" >> "$GITHUB_OUTPUT"
          echo "encrypted_staging_v2_diagnostic=success"

      - name: Upload ciphertext only
        uses: actions/upload-artifact@v7
        with:
          name: goanime-manga-staging-v2-diagnostic-encrypted
          path: ${{ steps.encrypt.outputs.transfer }}
          compression-level: 0
          retention-days: 1
          if-no-files-found: error

      - name: Remove private checkout and transient data
        if: always()
        run: rm -rf "$GITHUB_WORKSPACE/private-source" "$GITHUB_WORKSPACE/toolchain-control" "$RUNNER_TEMP/goanime-manga-staging-v2-diagnostic"
