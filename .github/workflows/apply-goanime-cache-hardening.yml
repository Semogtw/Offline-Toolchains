name: Retired GoAnime Dart cache hardening apply

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-24.04
    steps:
      - name: Retired workflow
        run: |
          echo 'This one-shot Dart cache hardening apply workflow is retired.'
          echo 'Provider crawling now belongs to .github/workflows/goanime-scrapling-provider-cache.yml.'
