name: Apply GoAnime Manga download source race fix

on:
  push:
    branches: [main]
    paths:
      - 'triggers/goanime-manga-download-source-race-20260822/*.request'

permissions:
  contents: read

concurrency:
  group: goanime-manga-download-source-race-20260822
  cancel-in-progress: false

jobs:
  apply:
    if: github.actor == github.repository_owner
    runs-on: ubuntu-24.04
    timeout-minutes: 45
    steps:
      - name: Require private repository token
        env:
          PRIVATE_REPOSITORIES_TOKEN: ${{ secrets.PRIVATE_REPOSITORIES_TOKEN }}
        run: test -n "$PRIVATE_REPOSITORIES_TOKEN"

      - name: Checkout trusted Toolchains source
        uses: actions/checkout@v6
        with:
          path: trusted-source
          fetch-depth: 2
          persist-credentials: false
          show-progress: false

      - name: Resolve exact GoAnime revision
        id: request
        shell: bash
        run: |
          set -euo pipefail
          request="$(git -C trusted-source diff-tree --no-commit-id --name-only --diff-filter=A -r HEAD^ HEAD -- 'triggers/goanime-manga-download-source-race-20260822/*.request' | head -n 1)"
          test -n "$request"
          read -r project branch sha < "trusted-source/$request"
          test "$project" = goanime
          test "$branch" = 'feature/manga-platform'
          [[ "$sha" =~ ^[0-9a-f]{40}$ ]]
          printf 'branch=%s\nsha=%s\n' "$branch" "$sha" >> "$GITHUB_OUTPUT"

      - name: Checkout exact private revision
        uses: actions/checkout@v6
        with:
          repository: Semogtw/goanime-mobile
          ref: ${{ steps.request.outputs.sha }}
          token: ${{ secrets.PRIVATE_REPOSITORIES_TOKEN }}
          path: private-source
          fetch-depth: 0
          persist-credentials: false
          show-progress: false

      - name: Verify current remote head
        working-directory: private-source
        env:
          PRIVATE_REPOSITORIES_TOKEN: ${{ secrets.PRIVATE_REPOSITORIES_TOKEN }}
          EXPECTED_SHA: ${{ steps.request.outputs.sha }}
          TARGET_BRANCH: ${{ steps.request.outputs.branch }}
        shell: bash
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = "$EXPECTED_SHA"
          auth="$(printf 'x-access-token:%s' "$PRIVATE_REPOSITORIES_TOKEN" | base64 -w0)"
          remote="$(git -c http.extraheader="AUTHORIZATION: basic $auth" ls-remote https://github.com/Semogtw/goanime-mobile.git "refs/heads/$TARGET_BRANCH" | awk '{print $1}')"
          test "$remote" = "$EXPECTED_SHA"

      - name: Apply reviewed Downloads race patch
        working-directory: private-source
        run: |
          set -euo pipefail
          patch="$GITHUB_WORKSPACE/trusted-source/patches/goanime/manga-source-race-20260822.patch"
          git apply --check \
            --include='lib/screens/manga/manga_downloads_screen.dart' \
            --include='test/widgets/manga/manga_downloads_screen_test.dart' \
            "$patch"
          git apply \
            --include='lib/screens/manga/manga_downloads_screen.dart' \
            --include='test/widgets/manga/manga_downloads_screen_test.dart' \
            "$patch"
          git diff --check

      - name: Set up Flutter 3.44.1
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          flutter-version: 3.44.1
          cache: true

      - name: Resolve dependencies
        working-directory: private-source
        run: |
          set -euo pipefail
          flutter config --no-analytics
          flutter pub get
          (cd packages/goanime_core && dart pub get)

      - name: Format affected stabilization files
        working-directory: private-source
        run: |
          set -euo pipefail
          dart format \
            lib/screens/manga/manga_history_screen.dart \
            lib/screens/manga/manga_downloads_screen.dart \
            lib/widgets/manga/manga_browse_library_view.dart \
            lib/widgets/manga/manga_browse_categories_view.dart \
            lib/widgets/manga/manga_readable_search_view.dart \
            test/widgets/manga/manga_history_screen_test.dart \
            test/widgets/manga/manga_downloads_screen_test.dart \
            test/widgets/manga/manga_browse_library_view_test.dart \
            test/widgets/manga/manga_browse_categories_view_test.dart \
            test/widgets/manga/manga_readable_search_view_test.dart \
            test/widgets/manga/manga_reader_screen_test.dart >/dev/null
          git diff --check

      - name: Validate current Manga stabilization
        working-directory: private-source
        run: |
          set -euo pipefail
          python3 -m unittest discover -s tools/tests -p 'test_*.py'
          python3 tools/validate_repository_guards.py
          bash tools/validate_release_workflows.sh
          bash tools/validate_project_health.sh
          flutter analyze --no-pub
          flutter test --no-pub --concurrency=1 \
            test/widgets/manga/manga_browse_library_view_test.dart \
            test/widgets/manga/manga_browse_categories_view_test.dart \
            test/widgets/manga/manga_readable_search_view_test.dart \
            test/widgets/manga/manga_reader_screen_test.dart \
            test/widgets/manga/manga_details_library_test.dart \
            test/widgets/manga/manga_history_screen_test.dart \
            test/widgets/manga/manga_downloads_screen_test.dart

      - name: Commit validated checkpoint
        working-directory: private-source
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add \
            lib/screens/manga/manga_history_screen.dart \
            lib/screens/manga/manga_downloads_screen.dart \
            lib/widgets/manga/manga_browse_library_view.dart \
            lib/widgets/manga/manga_browse_categories_view.dart \
            lib/widgets/manga/manga_readable_search_view.dart \
            test/widgets/manga/manga_history_screen_test.dart \
            test/widgets/manga/manga_downloads_screen_test.dart \
            test/widgets/manga/manga_browse_library_view_test.dart \
            test/widgets/manga/manga_browse_categories_view_test.dart \
            test/widgets/manga/manga_readable_search_view_test.dart \
            test/widgets/manga/manga_reader_screen_test.dart
          git commit -m 'fix(manga): prevent stale download source replacement'
          test -z "$(git status --porcelain)"

      - name: Fast-forward private Manga branch
        id: publish
        working-directory: private-source
        env:
          PRIVATE_REPOSITORIES_TOKEN: ${{ secrets.PRIVATE_REPOSITORIES_TOKEN }}
          EXPECTED_SHA: ${{ steps.request.outputs.sha }}
          TARGET_BRANCH: ${{ steps.request.outputs.branch }}
        run: |
          set -euo pipefail
          new_sha="$(git rev-parse HEAD)"
          auth="$(printf 'x-access-token:%s' "$PRIVATE_REPOSITORIES_TOKEN" | base64 -w0)"
          remote="$(git -c http.extraheader="AUTHORIZATION: basic $auth" ls-remote https://github.com/Semogtw/goanime-mobile.git "refs/heads/$TARGET_BRANCH" | awk '{print $1}')"
          test "$remote" = "$EXPECTED_SHA"
          git -c http.extraheader="AUTHORIZATION: basic $auth" push https://github.com/Semogtw/goanime-mobile.git "HEAD:refs/heads/$TARGET_BRANCH"
          echo "sha=$new_sha" >> "$GITHUB_OUTPUT"

      - name: Summary
        run: echo "Published Manga download race SHA ${{ steps.publish.outputs.sha }}" >> "$GITHUB_STEP_SUMMARY"

      - name: Remove private checkout
        if: always()
        run: rm -rf "$GITHUB_WORKSPACE/private-source" "$GITHUB_WORKSPACE/trusted-source"
