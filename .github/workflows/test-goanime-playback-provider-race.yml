name: GoAnime playback provider race TDD

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/test-goanime-playback-provider-race.yml'

permissions:
  contents: read

concurrency:
  group: goanime-playback-provider-race
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - name: Checkout exact GoAnime source
        uses: actions/checkout@v6
        with:
          repository: Semogtw/goanime-mobile
          ref: f434192de2f6d9d1d2d18d4171f20ab69e751a96
          token: ${{ secrets.PRIVATE_REPOSITORIES_TOKEN }}
          path: private-source
          fetch-depth: 1
          persist-credentials: false
          lfs: false
          submodules: false
          show-progress: false

      - name: Verify immutable source
        working-directory: private-source
        shell: bash
        run: test "$(git rev-parse HEAD)" = 'f434192de2f6d9d1d2d18d4171f20ab69e751a96'

      - name: Set up Flutter 3.44.1
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          flutter-version: 3.44.1
          cache: true

      - name: Resolve dependencies
        working-directory: private-source
        shell: bash
        run: |
          set -euo pipefail
          flutter config --no-analytics
          flutter pub get

      - name: Run playback resolver regression tests
        working-directory: private-source
        shell: bash
        run: >-
          flutter test --no-pub
          test/services/video_playback_resolver_test.dart
          test/services/video_playback_resolver_late_provider_test.dart
