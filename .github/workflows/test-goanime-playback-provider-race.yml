name: GoAnime playback provider race TDD

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/test-goanime-playback-provider-race.yml'

permissions:
  contents: read

concurrency:
  group: goanime-playback-provider-race
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-24.04
    timeout-minutes: 20
    steps:
      - name: Checkout exact GoAnime source
        uses: actions/checkout@v6
        with:
          repository: Semogtw/goanime-mobile
          ref: 044db058541052e4be8df0db8f03ab0e31d9006c
          token: ${{ secrets.PRIVATE_REPOSITORIES_TOKEN }}
          path: private-source
          fetch-depth: 1
          persist-credentials: false
          lfs: false
          submodules: false
          show-progress: false

      - name: Verify immutable source
        working-directory: private-source
        shell: bash
        run: test "$(git rev-parse HEAD)" = '044db058541052e4be8df0db8f03ab0e31d9006c'

      - name: Set up Flutter 3.44.1
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          flutter-version: 3.44.1
          cache: true

      - name: Resolve dependencies
        working-directory: private-source
        shell: bash
        run: |
          set -euo pipefail
          flutter config --no-analytics
          flutter pub get

      - name: Run playback provider race regression tests
        working-directory: private-source
        shell: bash
        run: >-
          flutter test --no-pub
          test/services/video_playback_resolver_test.dart
          test/services/video_playback_resolver_late_provider_test.dart
          test/services/playback_provider_enrichment_service_test.dart
