name: Run GoAnime cache tooling diagnostics

on:
  push:
    branches: [main]
    paths:
      - 'triggers/goanime-cache-diagnostics/*.request'

permissions:
  contents: read

concurrency:
  group: goanime-cache-tooling-diagnostics
  cancel-in-progress: true

jobs:
  diagnose:
    if: github.actor == github.repository_owner
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - name: Checkout trusted Toolchains source
        uses: actions/checkout@v6
        with:
          path: trusted-source
          fetch-depth: 2
          persist-credentials: false
          show-progress: false

      - name: Resolve exact GoAnime revision
        id: request
        shell: bash
        run: |
          set -euo pipefail
          request="$(git -C trusted-source diff-tree --no-commit-id --name-only --diff-filter=A -r HEAD^ HEAD -- 'triggers/goanime-cache-diagnostics/*.request' | head -n 1)"
          test -n "$request"
          read -r project branch sha < "trusted-source/$request"
          test "$project" = 'goanime'
          test "$branch" = 'main'
          [[ "$sha" =~ ^[0-9a-f]{40}$ ]]
          echo "sha=$sha" >> "$GITHUB_OUTPUT"

      - name: Require read-only private repository token
        shell: bash
        env:
          PRIVATE_REPOSITORIES_TOKEN: ${{ secrets.PRIVATE_REPOSITORIES_TOKEN }}
        run: test -n "$PRIVATE_REPOSITORIES_TOKEN"

      - name: Checkout exact private GoAnime revision
        uses: actions/checkout@v6
        with:
          repository: Semogtw/goanime-mobile
          ref: ${{ steps.request.outputs.sha }}
          token: ${{ secrets.PRIVATE_REPOSITORIES_TOKEN }}
          path: private-source
          fetch-depth: 0
          persist-credentials: false
          lfs: false
          submodules: false
          show-progress: false

      - name: Verify exact source
        working-directory: private-source
        shell: bash
        run: test "$(git rev-parse HEAD)" = '${{ steps.request.outputs.sha }}'

      - name: Set up Flutter 3.44.1
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          flutter-version: 3.44.1
          cache: true

      - name: Resolve dependencies
        working-directory: private-source
        shell: bash
        run: |
          set -euo pipefail
          flutter config --no-analytics
          flutter pub get
          (cd packages/goanime_core && dart pub get)

      - name: Analyze provider catalog executable
        working-directory: private-source
        run: dart analyze tools/provider_catalog_cache.dart

      - name: Analyze MAL provider executable
        working-directory: private-source
        run: dart analyze tools/build_mal_provider_availability_cache.dart

      - name: Goyabu parses catalog and pagination
        continue-on-error: true
        working-directory: private-source
        run: flutter test --no-pub test/tools/provider_catalog_cache_test.dart --plain-name 'extracts anime entries, audio modes and all pagination pages'

      - name: Goyabu rejects lookalikes
        continue-on-error: true
        working-directory: private-source
        run: flutter test --no-pub test/tools/provider_catalog_cache_test.dart --plain-name 'rejects lookalike external anime links'

      - name: AniTube parses legendados index
        continue-on-error: true
        working-directory: private-source
        run: flutter test --no-pub test/tools/provider_catalog_cache_test.dart --plain-name 'parses the alphabetical legendados index'

      - name: AniTube merges dubbed variants
        continue-on-error: true
        working-directory: private-source
        run: flutter test --no-pub test/tools/provider_catalog_cache_test.dart --plain-name 'merges dubbed variants into the same normalized title'

      - name: AniTube discovers pagination
        continue-on-error: true
        working-directory: private-source
        run: flutter test --no-pub test/tools/provider_catalog_cache_test.dart --plain-name 'discovers explicit pagination without following anime detail pages'

      - name: Snapshot accepts healthy refresh
        continue-on-error: true
        working-directory: private-source
        run: flutter test --no-pub test/tools/provider_catalog_cache_test.dart --plain-name 'accepts a complete healthy provider refresh'

      - name: Snapshot preserves partial crawl
        continue-on-error: true
        working-directory: private-source
        run: flutter test --no-pub test/tools/provider_catalog_cache_test.dart --plain-name 'preserves the last complete provider snapshot after partial crawl'

      - name: Snapshot rejects suspicious replacement
        continue-on-error: true
        working-directory: private-source
        run: flutter test --no-pub test/tools/provider_catalog_cache_test.dart --plain-name 'rejects a suspiciously small replacement even if crawl says complete'

      - name: Manifest exposes every provider
        continue-on-error: true
        working-directory: private-source
        run: flutter test --no-pub test/tools/provider_catalog_cache_test.dart --plain-name 'manifest exposes independent status and counts for every provider'

      - name: Test MAL provider provenance tooling
        working-directory: private-source
        run: flutter test --no-pub --reporter expanded test/tools/build_mal_provider_availability_cache_test.dart

      - name: Remove private checkout
        if: always()
        shell: bash
        run: rm -rf "$GITHUB_WORKSPACE/private-source" "$GITHUB_WORKSPACE/trusted-source"
