name: Run GoAnime cache tooling diagnostics

on:
  push:
    branches: [main]
    paths:
      - 'triggers/goanime-cache-diagnostics/*.request'

permissions:
  contents: read

concurrency:
  group: goanime-cache-tooling-diagnostics
  cancel-in-progress: true

jobs:
  diagnose:
    if: github.actor == github.repository_owner
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - name: Checkout trusted Toolchains source
        uses: actions/checkout@v6
        with:
          path: trusted-source
          fetch-depth: 2
          persist-credentials: false
          show-progress: false

      - name: Resolve exact GoAnime revision
        id: request
        shell: bash
        run: |
          set -euo pipefail
          request="$(git -C trusted-source diff-tree --no-commit-id --name-only --diff-filter=A -r HEAD^ HEAD -- 'triggers/goanime-cache-diagnostics/*.request' | head -n 1)"
          test -n "$request"
          read -r project branch sha < "trusted-source/$request"
          test "$project" = 'goanime'
          test "$branch" = 'main'
          [[ "$sha" =~ ^[0-9a-f]{40}$ ]]
          echo "sha=$sha" >> "$GITHUB_OUTPUT"

      - name: Require read-only private repository token
        shell: bash
        env:
          PRIVATE_REPOSITORIES_TOKEN: ${{ secrets.PRIVATE_REPOSITORIES_TOKEN }}
        run: test -n "$PRIVATE_REPOSITORIES_TOKEN"

      - name: Checkout exact private GoAnime revision
        uses: actions/checkout@v6
        with:
          repository: Semogtw/goanime-mobile
          ref: ${{ steps.request.outputs.sha }}
          token: ${{ secrets.PRIVATE_REPOSITORIES_TOKEN }}
          path: private-source
          fetch-depth: 0
          persist-credentials: false
          lfs: false
          submodules: false
          show-progress: false

      - name: Verify exact source
        working-directory: private-source
        shell: bash
        run: test "$(git rev-parse HEAD)" = '${{ steps.request.outputs.sha }}'

      - name: Set up Flutter 3.44.1
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          flutter-version: 3.44.1
          cache: true

      - name: Resolve dependencies
        working-directory: private-source
        shell: bash
        run: |
          set -euo pipefail
          flutter config --no-analytics
          flutter pub get
          (cd packages/goanime_core && dart pub get)

      - name: Analyze cache executables
        working-directory: private-source
        shell: bash
        run: |
          set -euo pipefail
          dart analyze tools/provider_catalog_cache.dart
          dart analyze tools/build_mal_provider_availability_cache.dart
          dart analyze tools/super_animes_catalog_metadata.dart
          dart analyze tools/anilist_mal_fallback.dart

      - name: Test provider catalog tooling
        working-directory: private-source
        shell: bash
        run: |
          set -euo pipefail
          flutter test --no-pub --concurrency=1 --reporter expanded test/tools/provider_catalog_cache_test.dart test/tools/goyabu_catalog_regression_test.dart
          flutter test --no-pub --concurrency=1 --test-randomize-ordering-seed=1701 test/tools/provider_catalog_cache_test.dart test/tools/goyabu_catalog_regression_test.dart

      - name: Test MAL provider provenance tooling
        working-directory: private-source
        run: flutter test --no-pub --reporter expanded test/tools/build_mal_provider_availability_cache_test.dart

      - name: Test AniList MAL fallback tooling
        working-directory: private-source
        shell: bash
        run: flutter test --no-pub --reporter expanded test/tools/anilist_mal_fallback_test.dart

      - name: Test Super Animes metadata tooling
        working-directory: private-source
        shell: bash
        run: flutter test --no-pub --reporter expanded test/tools/super_animes_catalog_metadata_test.dart

      - name: Live crawler smoke audit
        working-directory: private-source
        shell: bash
        env:
          CACHE_BUILDER_MAX_RUNTIME_MINUTES: '5'
        run: |
          set -euo pipefail
          dart run tools/provider_catalog_cache.dart --minimum-provider-entries=1

      - name: Production provider catalog audit
        working-directory: private-source
        shell: bash
        env:
          CACHE_BUILDER_MAX_RUNTIME_MINUTES: '5'
        run: |
          set -euo pipefail
          dart run tools/provider_catalog_cache.dart >/dev/null
          python3 - <<'PY'
          import json
          from pathlib import Path

          manifest = json.loads(Path('assets/data/anime_provider_catalog_manifest.json').read_text(encoding='utf-8'))
          providers = manifest.get('providers') or {}
          required = ('animefire', 'animesonline', 'goyabu', 'anitube')
          missing = [name for name in required if name not in providers]
          if missing:
              raise SystemExit(f'missing provider manifest entries: {missing}')
          for provider in required:
              info = providers[provider]
              status = str(info.get('status') or 'missing')
              count = int(info.get('count') or 0)
              attempted = int(info.get('attemptedUrls') or 0)
              successful = int(info.get('successfulUrls') or 0)
              print(f'::notice title=GoAnime provider audit::{provider}: status={status}, titles={count}, urls={successful}/{attempted}')
          print(f'::notice title=GoAnime catalog union::titles={int(manifest.get("availableTitleCount") or 0)}, allProvidersComplete={bool(manifest.get("allProvidersComplete"))}')
          PY

      - name: Remove private checkout
        if: always()
        shell: bash
        run: rm -rf "$GITHUB_WORKSPACE/private-source" "$GITHUB_WORKSPACE/trusted-source"