name: Validate GoAnime Progress privately

on:
  push:
    branches:
      - main
    paths:
      - 'triggers/goanime-progress-private/*.request'

permissions:
  contents: read

concurrency:
  group: goanime-progress-private
  cancel-in-progress: false

jobs:
  validate:
    name: Validate shared Progress events
    if: github.actor == github.repository_owner
    runs-on: ubuntu-24.04
    timeout-minutes: 30

    steps:
      - name: Require private repository token
        env:
          PRIVATE_REPOSITORIES_TOKEN: ${{ secrets.PRIVATE_REPOSITORIES_TOKEN }}
        run: test -n "$PRIVATE_REPOSITORIES_TOKEN"

      - name: Checkout toolchain request
        uses: actions/checkout@v6
        with:
          path: control
          fetch-depth: 2
          persist-credentials: false
          show-progress: false

      - name: Resolve exact source revision
        id: request
        shell: bash
        run: |
          set -euo pipefail
          request="$(git -C control diff-tree --no-commit-id --name-only --diff-filter=A -r HEAD^ HEAD -- 'triggers/goanime-progress-private/*.request' | head -n 1)"
          test -n "$request"
          read -r project branch sha < "control/$request"
          test "$project" = "goanime"
          [[ "$branch" =~ ^[A-Za-z0-9._/-]{1,200}$ ]]
          [[ "$branch" != /* ]]
          [[ "$branch" != */ ]]
          [[ "$branch" != *..* ]]
          [[ "$sha" =~ ^[0-9a-f]{40}$ ]]
          echo "sha=$sha" >> "$GITHUB_OUTPUT"

      - name: Checkout exact private source
        uses: actions/checkout@v6
        with:
          repository: Semogtw/goanime-mobile
          ref: ${{ steps.request.outputs.sha }}
          token: ${{ secrets.PRIVATE_REPOSITORIES_TOKEN }}
          path: private-source
          fetch-depth: 1
          persist-credentials: false
          lfs: false
          submodules: false
          show-progress: false

      - name: Set up Flutter 3.44.1
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          flutter-version: 3.44.1
          cache: true

      - name: Resolve dependencies privately
        working-directory: private-source
        shell: bash
        run: |
          set -euo pipefail
          flutter pub get >/dev/null 2>&1
          (cd packages/goanime_core && dart pub get >/dev/null 2>&1)

      - name: Analyze Progress slice
        working-directory: private-source
        shell: bash
        run: |
          set -euo pipefail
          flutter analyze --no-pub \
            lib/services/media/anime_progress_event_adapter.dart \
            lib/services/media/manga_progress_event_adapter.dart \
            lib/services/media/anime_user_state_event_adapter.dart \
            test/services/media/anime_progress_event_adapter_test.dart \
            test/services/media/manga_progress_event_adapter_test.dart \
            test/services/media/anime_user_state_event_adapter_test.dart \
            test/architecture/shared_progress_event_guard_test.dart
          (cd packages/goanime_core && dart analyze \
            lib/src/media/media_progress_event.dart \
            lib/src/media/user_media_state_event.dart \
            test/media/media_progress_event_test.dart \
            test/media/user_media_state_event_test.dart)

      - name: Run core Progress event tests
        working-directory: private-source/packages/goanime_core
        run: dart test test/media/media_progress_event_test.dart

      - name: Run core User state envelope tests
        working-directory: private-source/packages/goanime_core
        run: dart test test/media/user_media_state_event_test.dart

      - name: Run Anime Progress adapter tests
        working-directory: private-source
        run: flutter test --no-pub test/services/media/anime_progress_event_adapter_test.dart

      - name: Run Manga Progress adapter tests
        working-directory: private-source
        run: flutter test --no-pub test/services/media/manga_progress_event_adapter_test.dart

      - name: Run Anime user-state adapter tests
        working-directory: private-source
        run: flutter test --no-pub test/services/media/anime_user_state_event_adapter_test.dart

      - name: Enforce Progress payload boundaries
        working-directory: private-source
        run: flutter test --no-pub test/architecture/shared_progress_event_guard_test.dart

      - name: Remove private checkout
        if: always()
        run: rm -rf "$GITHUB_WORKSPACE/private-source" "$GITHUB_WORKSPACE/control"

  report:
    name: Publish sanitized GoAnime Progress result
    if: always()
    needs: [validate]
    runs-on: ubuntu-24.04
    timeout-minutes: 5
    permissions:
      contents: read
    steps:
      - name: Publish sanitized GoAnime Progress result
        continue-on-error: true
        shell: bash
        env:
          GH_TOKEN: ${{ secrets.PRIVATE_REPOSITORIES_TOKEN }}
          VALIDATE_RESULT: ${{ needs.validate.result }}
          TOOLCHAINS_TRIGGER_SHA: ${{ github.sha }}
          RUN_ID: ${{ github.run_id }}
          RUN_ATTEMPT: ${{ github.run_attempt }}
        run: |
          set -euo pipefail
          report="$RUNNER_TEMP/goanime-progress-report.json"
          python3 - "$report" <<'PY'
          import json
          import os
          import sys
          from datetime import datetime, timezone

          payload = {
              "schemaVersion": 1,
              "runId": int(os.environ["RUN_ID"]),
              "runAttempt": int(os.environ["RUN_ATTEMPT"]),
              "toolchainsTriggerSha": os.environ["TOOLCHAINS_TRIGGER_SHA"],
              "validateResult": os.environ["VALIDATE_RESULT"],
              "finishedAt": datetime.now(timezone.utc).isoformat().replace("+00:00", "Z"),
          }
          with open(sys.argv[1], "w", encoding="utf-8") as stream:
              json.dump(payload, stream, indent=2, sort_keys=True)
              stream.write("\n")
          PY
          encoded="$(base64 -w0 < "$report")"
          path="reports/goanime-progress-private/${RUN_ID}.json"
          gh api --method PUT "repos/${GITHUB_REPOSITORY}/contents/${path}" \
            -f "message=ci(goanime): report Progress run ${RUN_ID}" \
            -f "content=${encoded}" >/dev/null
