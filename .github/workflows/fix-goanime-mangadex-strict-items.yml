name: Fix GoAnime MangaDex strict response resources

on:
  push:
    branches: [main]
    paths:
      - 'triggers/goanime-mangadex-strict-items-fix/*.request'

permissions:
  contents: read

concurrency:
  group: goanime-mangadex-strict-items-fix
  cancel-in-progress: false

jobs:
  fix:
    name: Reject malformed MangaDex response resources
    if: github.actor == github.repository_owner
    runs-on: ubuntu-24.04
    timeout-minutes: 30
    steps:
      - name: Checkout control repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 2
          persist-credentials: false
          show-progress: false

      - name: Set up Flutter 3.44.1
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          flutter-version: 3.44.1
          cache: true

      - name: Apply exact-SHA strict MangaDex response-resource fix
        env:
          PRIVATE_REPOSITORIES_TOKEN: ${{ secrets.PRIVATE_REPOSITORIES_TOKEN }}
        run: bash scripts/fix-goanime-mangadex-strict-items-v2.sh
