name: Patch Hydra Drive V2 audit findings

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled-cleanup-window:
    if: ${{ false }}
    runs-on: ubuntu-24.04
    steps:
      - run: echo "temporarily disabled while removing one-off audit requests"
