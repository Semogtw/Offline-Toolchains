# GoAnime offline toolchain refresh

This marker keeps rebuild requests explicit and auditable without changing the dependency fixture.

Requested for `Semogtw/goanime-mobile` maintenance on 2026-08-02 with:

- Flutter 3.44.1;
- Dart 3.12.1;
- the dependency fixture and Android caches defined by `build-goanime.yml`.

Update this marker only when the existing one-day artifact has expired and a fresh exact bundle is required.
