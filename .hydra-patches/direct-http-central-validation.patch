diff --git a/src/main/services/download/direct-http-real-smoke.electron-check.ts b/src/main/services/download/direct-http-real-smoke.electron-check.ts
index 6c43da71..0e797356 100644
--- a/src/main/services/download/direct-http-real-smoke.electron-check.ts
+++ b/src/main/services/download/direct-http-real-smoke.electron-check.ts
@@ -38,26 +38,43 @@ async function waitForDownloadedFile(filePath: string, timeoutMs = 20_000) {
   );
 }
 
-async function provePrivateDestinationsAreRejected() {
+async function provePrivateDestinationsAreRejected(downloadPath: string) {
+  const privateUri = "http://127.0.0.1:9/fixture.bin";
+
+  await assert.rejects(
+    () => validateDirectHttpDownloadUri(Downloader.DirectHttp, privateUri),
+    /Private or reserved destination is not allowed/
+  );
+
+  const privateDownload = {
+    shop: "steam",
+    objectId: "999999998",
+    uri: privateUri,
+    downloader: Downloader.DirectHttp,
+    downloadPath,
+    folderName: null,
+  } as Download;
+
   await assert.rejects(
-    () =>
-      validateDirectHttpDownloadUri(
-        Downloader.DirectHttp,
-        "http://127.0.0.1:9/fixture.bin"
-      ),
+    () => DownloadManager.resumeDownload(privateDownload),
     /Private or reserved destination is not allowed/
   );
+
+  if (DownloadManager.isJsDownloadActive) {
+    throw new Error("Rejected DirectHttp resume left a JS download active");
+  }
 }
 
 export async function runDirectHttpDownloadManagerSmoke() {
-  await provePrivateDestinationsAreRejected();
+  const downloadPath = await mkdtemp(path.join(os.tmpdir(), "hydra-direct-http-"));
+  const filePath = path.join(downloadPath, fixtureFilename);
+
+  await provePrivateDestinationsAreRejected(downloadPath);
 
   const safeUri = await validateDirectHttpDownloadUri(
     Downloader.DirectHttp,
     fixtureUrl
   );
-  const downloadPath = await mkdtemp(path.join(os.tmpdir(), "hydra-direct-http-"));
-  const filePath = path.join(downloadPath, fixtureFilename);
 
   const download = {
     shop: "steam",
@@ -80,6 +97,7 @@ export async function runDirectHttpDownloadManagerSmoke() {
     const report = {
       downloader: "DirectHttp",
       privateDestinationRejected: true,
+      privateResumeRejected: true,
       preflight: "ok",
       startDownload: "ok",
       bytes,
diff --git a/src/main/services/download/download-manager.ts b/src/main/services/download/download-manager.ts
index 570a10e3..f295796a 100644
--- a/src/main/services/download/download-manager.ts
+++ b/src/main/services/download/download-manager.ts
@@ -45,6 +45,7 @@ import {
 import { shouldFinalizeDownload } from "./download-completion";
 import { normalizeDownloadRelativePath } from "./download-path-policy";
 import { resolveExtractionSourcePath } from "./extraction-target-policy";
+import { validateDirectHttpDownloadUri } from "./validate-direct-http-download-uri";
 
 interface AllDebridBatchEntry {
   url: string;
@@ -1123,22 +1124,10 @@ export class DownloadManager {
       case Downloader.Rootz:
         return this.getRootzDownloadOptions(download, resumingFilename);
       case Downloader.DirectHttp: {
-        let parsed: URL;
-        try {
-          parsed = new URL(download.uri);
-        } catch {
-          throw new Error("Invalid direct HTTP download URL");
-        }
-
-        if (
-          !["http:", "https:"].includes(parsed.protocol) ||
-          parsed.username ||
-          parsed.password
-        ) {
-          throw new Error("Invalid direct HTTP download URL");
-        }
-
-        const directUrl = parsed.toString();
+        const directUrl = await validateDirectHttpDownloadUri(
+          download.downloader,
+          download.uri
+        );
         const filename = this.resolveFilename(
           resumingFilename,
           download.uri,
